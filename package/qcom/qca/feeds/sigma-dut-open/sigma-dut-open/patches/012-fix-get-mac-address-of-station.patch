--- a/sta.c
+++ b/sta.c
@@ -1743,7 +1743,7 @@ static enum sigma_cmd_result cmd_sta_get
 						     struct sigma_conn *conn,
 						     struct sigma_cmd *cmd)
 {
-	/* const char *intf = get_param(cmd, "Interface"); */
+	const char *intf = get_param(cmd, "Interface");
 	char addr[20], resp[50];
	const char *ap_link_mac = get_param(cmd, "AP_Link_MAC");
 
@@ -1757,8 +1757,7 @@ static enum sigma_cmd_result cmd_sta_get
					     sizeof(addr)) < 0)
			return -2;
	} else {
-		if (get_wpa_status(get_station_ifname(dut), "address",
-				   addr, sizeof(addr)) < 0)
+		if (get_wpa_status(intf, "address", addr, sizeof(addr)) < 0)
			return -2;
	}
 
@@ -17190,7 +17189,7 @@ static enum sigma_cmd_result cmd_sta_hs2
 	const char *intf = get_param(cmd, "Interface");
 	const char *display = get_param(cmd, "Display");
 	struct wpa_ctrl *ctrl;
-	char buf[300], params[400], *pos;
+	char buf[300], params[400], *pos = NULL;
 	char bssid[20];
 	int info_avail = 0;
 	unsigned int old_timeout;
